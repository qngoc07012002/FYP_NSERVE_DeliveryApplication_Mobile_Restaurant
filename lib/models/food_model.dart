class Food {
  String? id;
  String name;
  String imageUrl;
  String description;
  bool isAvailable;
  double price;
  Food(this.id, this.name, this.imageUrl, this.description, this.isAvailable, this.price, );
}